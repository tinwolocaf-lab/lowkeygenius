# Implementation Plan

- [x] 1. Set up Mermaid integration and MermaidDiagram component
  - [x] 1.1 Install mermaid package and configure for React
    - Add mermaid dependency to package.json
    - Configure mermaid initialization with default theme settings
    - _Requirements: 1.1, 1.3_
  - [x] 1.2 Create MermaidDiagram component with rendering logic
    - Implement component that accepts mermaid code string
    - Render mermaid syntax to SVG using mermaid.render()
    - Handle async rendering with loading state
    - _Requirements: 1.1_
  - [x] 1.3 Implement error handling for invalid mermaid syntax
    - Catch mermaid parsing errors
    - Display user-friendly error message
    - Show original code block when rendering fails
    - _Requirements: 1.2_
  - [ ]* 1.4 Write property test for mermaid rendering
    - **Property 1: Mermaid rendering produces SVG output**
    - **Validates: Requirements 1.1**
  - [ ]* 1.5 Write property test for mermaid error handling
    - **Property 2: Mermaid error handling preserves original code**
    - **Validates: Requirements 1.2**
  - [x] 1.6 Implement theme-aware mermaid rendering
    - Subscribe to ThemeContext for theme changes
    - Update mermaid theme configuration on theme change
    - Re-render diagrams when theme changes
    - _Requirements: 1.3, 1.4_
  - [ ]* 1.7 Write property test for mermaid theme reactivity
    - **Property 3: Mermaid theme reactivity**
    - **Validates: Requirements 1.3, 1.4**

- [x] 2. Create unified MarkdownRenderer component
  - [x] 2.1 Create MarkdownRenderer component with mermaid support
    - Create new component at src/components/MarkdownRenderer.tsx
    - Configure ReactMarkdown with remarkGfm and rehypeHighlight
    - Add custom code block renderer that detects mermaid language
    - Route mermaid blocks to MermaidDiagram component
    - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - [ ]* 2.2 Write property test for markdown element rendering
    - **Property 4: Markdown element rendering completeness**
    - **Validates: Requirements 2.2**
  - [x] 2.3 Refactor CourseView to use MarkdownRenderer
    - Replace inline ReactMarkdown usage with MarkdownRenderer component
    - Pass required props (content, courseId, lessonId, etc.)
    - _Requirements: 2.1, 2.4_
  - [x] 2.4 Refactor LessonPreview to use MarkdownRenderer
    - Replace inline ReactMarkdown usage with MarkdownRenderer component
    - Ensure consistent styling with CourseView
    - _Requirements: 2.1, 2.4_
  - [x] 2.5 Refactor GenerateLessons to use MarkdownRenderer (if applicable)
    - Check if markdown rendering exists in GenerateLessons
    - Replace with MarkdownRenderer if present
    - _Requirements: 2.1, 2.4_

- [x] 3. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 4. Implement SelectionOverlayMenu component
  - [x] 4.1 Create SelectionOverlayMenu component
    - Create component at src/components/SelectionOverlayMenu.tsx
    - Accept position, selection, and callback props
    - Render floating menu with action buttons
    - _Requirements: 3.1, 4.1, 5.1_
  - [x] 4.2 Implement role-based menu options
    - Show "Save as Note" option for all users
    - Show "Edit" option only when isOwner is true
    - Style buttons consistently with application theme
    - _Requirements: 3.2, 3.4, 4.1_
  - [ ]* 4.3 Write property test for selection menu options
    - **Property 5: Selection menu displays role-appropriate options**
    - **Validates: Requirements 3.1, 3.2, 3.4, 4.1**
  - [x] 4.4 Implement menu positioning logic
    - Calculate position based on selection rect
    - Adjust position to keep menu within viewport bounds
    - Handle edge cases (selection near top/bottom/sides)
    - _Requirements: 5.3_
  - [ ]* 4.5 Write property test for menu positioning
    - **Property 9: Menu positioning within viewport bounds**
    - **Validates: Requirements 5.3**
  - [x] 4.6 Implement menu dismissal behavior
    - Close menu on click outside
    - Close menu on scroll
    - Close menu when selection is cleared
    - _Requirements: 5.2, 5.4_
  - [ ]* 4.7 Write property test for menu dismissal
    - **Property 8: Menu dismissal on external interaction**
    - **Validates: Requirements 5.2, 5.4**

- [x] 5. Integrate text selection handling into MarkdownRenderer
  - [x] 5.1 Add text selection detection to MarkdownRenderer
    - Listen for mouseup events on content container
    - Detect text selection using window.getSelection()
    - Calculate selection position for menu placement
    - _Requirements: 3.1, 4.1, 5.1_
  - [x] 5.2 Wire SelectionOverlayMenu to MarkdownRenderer
    - Show SelectionOverlayMenu when text is selected
    - Pass selection data and position to menu
    - Handle menu close callback
    - _Requirements: 3.1, 4.1, 5.1_
  - [x] 5.3 Pass ownership context to MarkdownRenderer
    - Add isOwner prop to MarkdownRenderer
    - Determine ownership by comparing course.owner_id with user.id
    - Pass isOwner to SelectionOverlayMenu
    - _Requirements: 3.1, 3.4_

- [ ] 6. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 7. Implement note-taking functionality
  - [ ] 7.1 Implement note save handler in MarkdownRenderer
    - Create saveNote function that calls Supabase
    - Insert note with user_id, course_id, lesson_id, snippet_markdown
    - Show success toast on save
    - _Requirements: 4.2, 4.3_
  - [ ]* 7.2 Write property test for note creation metadata
    - **Property 7: Note creation stores complete metadata**
    - **Validates: Requirements 4.2, 4.4**
  - [ ] 7.3 Implement error handling for note save
    - Catch save errors and display error toast
    - Allow user to retry failed saves
    - _Requirements: 4.5_
  - [ ] 7.4 Update Notes page to display context metadata
    - Modify query to join courses and lessons tables
    - Display course_title and lesson_title with each note
    - Style metadata as reference information
    - _Requirements: 4.4_

- [ ] 8. Implement inline editing functionality
  - [ ] 8.1 Create InlineEditor component
    - Create component at src/components/InlineEditor.tsx
    - Render textarea with original text
    - Provide Save and Cancel buttons
    - Position near the selection
    - _Requirements: 3.2, 3.3_
  - [ ] 8.2 Implement inline edit save logic
    - Calculate new markdown content with edited text
    - Call Supabase to update lesson markdown_content
    - Update local state on success
    - _Requirements: 3.3_
  - [ ]* 8.3 Write property test for inline edit round-trip
    - **Property 6: Inline edit round-trip consistency**
    - **Validates: Requirements 3.3**
  - [ ] 8.4 Implement error handling for inline edit
    - Catch save errors and display error toast
    - Preserve original content on failure
    - _Requirements: 3.5_
  - [ ] 8.5 Wire InlineEditor to SelectionOverlayMenu
    - Show InlineEditor when Edit is clicked
    - Pass original text and save callback
    - Close menu and editor on save/cancel
    - _Requirements: 3.2, 3.3_

- [ ] 9. Update CourseView with full integration
  - [ ] 9.1 Add ownership detection to CourseView
    - Compare course.owner_id with current user.id
    - Pass isOwner prop to MarkdownRenderer
    - _Requirements: 3.1, 3.4_
  - [ ] 9.2 Wire content update callback in CourseView
    - Implement onContentUpdate handler
    - Update local lesson state after edit
    - Refresh lesson data from database
    - _Requirements: 3.3_
  - [ ] 9.3 Pass course and lesson context to MarkdownRenderer
    - Pass courseId, lessonId, courseTitle, lessonTitle props
    - Enable note-taking with full context
    - _Requirements: 4.2_

- [ ] 10. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
